{
  "name": "php-sandbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "composer:diagnose": "docker run --rm -it -v $(pwd):/app composer diagnose",
    "composer:init": "docker run --rm -it -v $(pwd):/app composer init",
    "composer:install": "docker run --rm -it -v $(pwd):/app composer install",
    "composer:validate": "docker run --rm -it -v $(pwd):/app composer validate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thisishuey/php-sandbox.git"
  },
  "author": "Jeff \"Huey\" Huelsbeck",
  "license": "GPL-2.0-or-later",
  "bugs": {
    "url": "https://github.com/thisishuey/php-sandbox/issues"
  },
  "homepage": "https://github.com/thisishuey/php-sandbox#readme"
}
